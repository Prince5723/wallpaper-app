/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'plus.unsplash.com','panels-cdn.imgix.net','images.unsplash.com'],
      },
};

export default nextConfig;
